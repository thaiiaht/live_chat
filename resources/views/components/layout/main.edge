<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title || "Live Stream" }}</title>
  @vite(['resources/js/auth.js', 'resources/css/auth.css', 'resources/css/main.css' ])
  @if ($slots.styles)
    {{{ await $slots.styles() }}}
  @endif
</head>
<body>
  <div class="container">
    <div class="main-content">
      @include('partials/header')

      @if ($slots.content)
        {{{ await $slots.content() }}}
      @endif
    </div>
  </div>
  @if ($slots.scripts)
    {{{ await $slots.scripts() }}}
  @endif
</body>
</html>
